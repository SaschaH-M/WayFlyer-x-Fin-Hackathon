# Copy to .env and fill in to enable the optional LLM phrasing layer + Postgres.
# Without these the app still runs fully (SQLite + deterministic grounded agent).

# ANTHROPIC_API_KEY=sk-ant-...
# DATABASE_URL=postgresql://localhost/prettyfly
